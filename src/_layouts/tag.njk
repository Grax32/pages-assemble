<h1>{{ title }}</h1>
<p>{{ tagPosts.length }} article{% if tagPosts.length != 1 %}s{% endif %} found.</p>
<ul>
  {% for post in tagPosts %}
    <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
  {% endfor %}
</ul>
